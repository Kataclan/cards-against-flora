import { createMuiTheme } from '@material-ui/core';

export default createMuiTheme({
  palette: {
    primary: { main: '#8bc34a' },
    secondary: { main: '#8d6e63' },
    text: {
      primary: '#FFFFFF',
    },
  },
});
